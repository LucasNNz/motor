[project]
name = "image-motor-mvp"
version = "0.2.1"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn>=0.30.0",
  "pillow>=10.4.0",
  "requests>=2.32.0",
]

[tool.vercel]
entrypoint = "engine.server:app"
